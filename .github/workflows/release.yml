name: Release - Semantic Versioning

on:
  workflow_run:
    workflows:
      - CI - Quality and Tests
    types:
      - completed

jobs:
  versioning:
    name: Running Python Semantic Release
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      github.event.workflow_run.head_branch == 'main'
    permissions:
      contents: write
    runs-on: ubuntu-latest
    outputs:
      new_version: ${{ steps.get_version.outputs.version }}

    steps:
      - name: Checkout the repository code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
          ref: ${{ github.event.workflow_run.head_sha }}

      - name: Attach checked commit to workflow branch
        run: |
          git checkout -B "${{ github.event.workflow_run.head_branch }}" "${{ github.event.workflow_run.head_sha }}"

      - name: Release preflight diagnostics
        run: |
          echo "workflow head branch: ${{ github.event.workflow_run.head_branch }}"
          echo "workflow head sha: ${{ github.event.workflow_run.head_sha }}"
          echo "git branch --show-current: $(git branch --show-current)"
          echo "git rev-parse --abbrev-ref HEAD: $(git rev-parse --abbrev-ref HEAD)"
          echo "git symbolic-ref --short HEAD: $(git symbolic-ref -q --short HEAD || echo DETACHED)"

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.13"

      - name: Install Python Semantic Release
        run: pip install python-semantic-release

      - name: Run Semantic Release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: semantic-release -vv version

      - name: Get created version tag
        id: get_version
        run: |
          git fetch --tags
          VERSION=$(git describe --tags --abbrev=0)
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "Created version: $VERSION"